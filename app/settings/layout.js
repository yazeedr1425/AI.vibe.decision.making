// صفحة الإعدادات كومبوننت عميل، فما تقدر تصدّر metadata بنفسها.
export const metadata = {
  title: "الإعدادات — احسم",
  description: "اسمك، نبرة الرد، مزاجك الافتراضي، والقراءة الصوتية.",
};

export default function SettingsLayout({ children }) {
  return children;
}
