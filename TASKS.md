# حالة المشروع والمهام — «احسم»

آخر تحديث: 2026-08-14. الخطة الأصلية (فريق من ٣ في ٤ أسابيع على React + Vite) انتهى زمنها — البناء صار على Next.js بإيقاع مختلف، وهذا الملف صار **لوحة حالة**: المنجز، والمنتظر، وما قبل النشر.

---

## ✅ المنجز

### الأساس والحسابات
- [x] Next.js 16 + Tailwind 4 + هوية بصرية كاملة RTL
- [x] Supabase: المخطط بالمهاجرات المرتبة + RLS على كل الجداول
- [x] تسجيل حساب ودخول وخروج (مع تأكيد بضغطتين للخروج)
- [x] رسائل التحقق بقالب عربي عبر Mailtrap من نطاقنا — مع حذف تعويضي لو فشل الإرسال
- [x] البروفايل: الاسم + النبرة (مرح/جدي) + المزاج الافتراضي

### محرك القرار
- [x] إنشاء القرار: الفئة + من ٢ إلى ٥ خيارات + المزاج
- [x] الأسئلة الديناميكية حسب الفئة → أوزان
- [x] التقييم السريع بسلالم خاصة لكل معيار → درجات
- [x] التوصية من Gemini بشخصية احسم + الحساب المحلي كخط رجعة
- [x] «ليش هذا القرار؟» + «وضّح أكثر» + مود «أنا متردد جدًا»
- [x] سجل القرارات + «كان قرار صح؟» (جدول `feedback`)
- [x] «شخصيتك القرارية»: الأنماط تُحسب في الكود وGemini يفسّر
- [x] اقتراح الخيار الثالث أثناء الكتابة
- [x] تفكيك القرار الكبير: كاشف محلي + فحوصات + حكم «اقدم / مو الحين»

### القرار الجماعي
- [x] إنشاء برابط `/vote/:share_code` + باركود QR محلي + نسخ ومشاركة
- [x] تصويت الضيوف بالاسم بلا حسابات — عبر دوال RPC آمنة لا قراءة جداول
- [x] النتائج لايف (بث كجرس + إعادة جلب) + الحاضرون لحظياً
- [x] الإقفال بيد المنشئ + معلّق Gemini يعلن ويكسر التعادل
- [ ] عدّاد مهلة زمنية + إقفال تلقائي — *المخطط والسياسات جاهزة، الواجهة لم تُبنَ*
- [ ] صور الخيارات في Storage — *الـ bucket موجود، الرفع لم يُبنَ*

### وكلاء إضافيون
- [x] خطة اليوم: Places + Routes + طقس Open-Meteo
- [x] تحليل المخاطر: SWOT + شجرة مسارات (جدول `analyses`)

### التوثيق
- [x] README عربي + إنجليزي شاملان · صفحة «كيف يعمل» محدثة · PRD محدث

---

## ⏳ بانتظار قرار يزيد

- فرع `feat/devils-advocate` — محامي الخيار الخاسر، **جاهز ومختبَر**، ينتظر المراجعة
- فرع `feat/photo-options` — الخيارات من صورة، متوقف (حذف أم إحياء؟)
- فروع محلية قديمة للكنس: `feat/outcome-loop` (مدمج) · `feat/gemini-engine` · `design/framer-refresh` · `feat/mailtrap-activation`

## 🚀 قبل النشر على Vercel

- [ ] حدود معدل للمسارات القديمة: `decide` · `plan` · `analyze` (الأحدث كلها محمية)
- [ ] حذف `20260812020000_disable_rls_dev.sql` من المهاجرات — يعطّل RLS ولا يجوز تنفيذه أبداً
- [ ] متغيرات البيئة السبعة في Vercel قبل أول build
- [ ] ضبط Site URL وRedirect URLs في Supabase Auth على النطاق الحقيقي
- [ ] إكمال توثيق `yazeed.store` في Mailtrap (سجل DMARC) لدخول صندوق الوارد

## 💡 أفكار مرشحة (بالترتيب المقترح)

1. بريد يومي «أمس اخترت كذا — كان قرار صح؟» بأزرار تقييم مباشرة — يغذي شخصيتك القرارية
2. بطاقة مشاركة للنتيجة والتصويت (صورة OG جاهزة للواتساب والستوري)
3. مهلة التصويت الجماعي مع إقفال عند القراءة (بلا بنية إضافية)
4. تثبيت كتطبيق PWA

---

## طريقة العمل مع Git (كما تجري فعلاً)

- فرع لكل ميزة: `feat/…` أو `fix/…` أو `docs/…` من `main` محدث
- commit + push على الفرع، ويزيد يفتح الـ PR ويدمج — ثم يُحذف الفرع المحلي ويُكنس البعيد
- أي تغيير على القاعدة = ملف مهاجرة جديد بطابع زمني في `supabase/migrations/` — لا تعديل على ملفات قديمة
- `.env.local` لا يُرفع أبداً، والمفاتيح كلها في الخادم فقط
- **RLS يوقف كل شي عند تفعيله**: قائمة فاضية بلا خطأ = سياسة ناقصة غالباً، مو خطأ كود

## معيار «خلصنا» — الحالة

- [x] Auth يشتغل فعلياً
- [x] Database بدورة CRUD كاملة
- [x] Realtime: تصويت من جهاز ثانٍ يحرّك الشاشة الأولى لحظياً
- [ ] Storage: الـ bucket جاهز، رفع صور الخيارات لم يُبنَ
- [x] ~~Edge Functions~~ → استُبدلت بمسارات API في Next.js (قرار معماري، مو نقصاً)
- [x] RLS مفعّل ومُختبَر — والضيوف عبر دوال آمنة فقط
- [ ] المشروع منشور على رابط يفتح من الجوال
- [x] رابط تصويت جماعي يشتغل من جهاز ثانٍ بدون تسجيل دخول
- [x] `README.md` يشرح التشغيل من الصفر
