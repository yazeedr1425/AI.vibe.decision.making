# توزيع المهام والجدول الزمني — مشروع "احسم"

الفريق: 3 أشخاص · المدة: 4 أسابيع · التقسيم **حسب الميزة كاملة** (كل واحد يبني الواجهة + القاعدة لجزئه) وليس حسب الطبقة، عشان ما تتعطّلون على بعض.

---

## توزيع الأدوار

### 👤 الشخص الأول — الأساس والحسابات (Auth)
مسؤول عن كل شي يستخدمه الباقون، فيبدأ أول واحد.

- [ ] إنشاء مشروع React + Vite ورفعه على الريبو
- [ ] إنشاء مشروع Supabase وإعطاء الفريق صلاحية الوصول
- [ ] تنفيذ `schema.sql` في SQL Editor
- [ ] ملف `supabaseClient.js` + متغيرات البيئة `.env`
- [ ] صفحة تسجيل حساب + تسجيل دخول + تسجيل خروج
- [ ] حماية الصفحات (زائر غير مسجّل يُحوّل لصفحة الدخول)
- [ ] صفحة البروفايل: الاسم + اختيار النبرة (مرح / جدي)
- [ ] التصميم العام والهوية البصرية (ألوان، خطوط، مكوّنات مشتركة)

**المكوّن المطلوب تغطيته:** Authentication

---

### 👤 الشخص الثاني — محرك القرار (Database)
قلب المنتج: من إدخال الخيارات حتى ظهور التوصية.

- [ ] شاشة إنشاء قرار: العنوان + الفئة + من 2 إلى 5 خيارات
- [ ] الأسئلة الديناميكية (2–3 أسئلة تختلف حسب الفئة: وقت / ميزانية / مزاج)
- [ ] خوارزمية **Weighted Scoring** وحساب `options.score`
- [ ] عرض التوصية + جملة **"ليش هذا القرار؟"** المبنية على الأوزان الفعلية
- [ ] زر "وضّح أكثر" يفصّل الأوزان بجُمل مفهومة لا أرقام
- [ ] مود **"أنا متردد جداً"**: اختيار عشوائي **موزون** (الأعلى تقييماً حظه أكبر لكن غير مضمون)
- [ ] سجلّ القرارات السابقة + تقييم الرضا (جدول `feedback`)
- [ ] نبرة الردود تتبع إعداد المستخدم (مرح / جدي)

**المكوّن المطلوب تغطيته:** Database (CRUD كامل)

---

### 👤 الشخص الثالث — القرار الجماعي والبنية الخلفية
أثقل جزء تقنياً، لكنه أقوى جزء في العرض التقديمي.

- [ ] رفع صور الخيارات إلى **Storage** وعرضها في البطاقات
- [ ] توليد رابط المشاركة `/vote/:share_code`
- [ ] صفحة تصويت عامة **بدون تسجيل دخول** (اسم فقط)
- [ ] **Realtime**: النتائج تتحرك لحظياً مع كل صوت
- [ ] عدّاد المهلة الزمنية في الواجهة
- [ ] **Edge Function**: إقفال التصويت عند انتهاء المهلة + تحديد الفائز
- [ ] النشر على Vercel وربطه بالريبو

**المكوّنات المطلوب تغطيتها:** Storage · Realtime · Edge Functions

---

## الجدول الزمني

| الأسبوع | الشخص ١ | الشخص ٢ | الشخص ٣ |
|---|---|---|---|
| **١** | إعداد المشروع + Supabase + تسجيل الدخول | مخطط الأسئلة والأوزان على ورق + شاشة إنشاء القرار | تجربة Storage و Realtime على مثال بسيط |
| **٢** | البروفايل + النبرة + التصميم العام | الخوارزمية + التوصية + "ليش هذا القرار؟" | رفع الصور + صفحة التصويت العامة |
| **٣** | تحسين الواجهة + مراجعة RLS | مود "أنا متردد جداً" + سجلّ القرارات | Realtime + Edge Function للإقفال |
| **٤** | اختبار شامل + إصلاح الأخطاء | تجهيز العرض التقديمي | النشر على Vercel |

---

## طريقة العمل مع Git

كل شخص يشتغل على فرع خاص، ولا أحد يرفع مباشرة على `main`:

```bash
git checkout main && git pull && git checkout -b feature/auth
```

بعد الانتهاء من الميزة:

```bash
git add . && git commit -m "add login page" && git push -u origin feature/auth
```

ثم Pull Request على GitHub، **يراجعه شخص ثاني** قبل الدمج.

أسماء الفروع المقترحة: `feature/auth` · `feature/decision-engine` · `feature/group-voting`

---

## قواعد تمنع مشاكل متكرّرة

1. **لا أحد يعدّل `schema.sql` بمفرده.** أي تغيير على الجداول يُناقش أولاً ثم يُكتب في الملف ويُرفع بـ PR — وإلا تنكسر قاعدة البيانات على الباقين.
2. **`.env` لا يُرفع أبداً.** موجود في `.gitignore`؛ المفاتيح تُتبادل خارج الريبو.
3. **RLS يوقف كل شي عند تفعيله.** إذا رجعت لك قائمة فاضية بدون خطأ، غالباً السبب سياسة ناقصة وليس خطأ في الكود.
4. **ابدأوا بميزة واحدة كاملة من البداية للنهاية** قبل الانتقال للي بعدها.
5. **Realtime يحتاج تفعيل للجدول** — مغطّى في `schema.sql`، لكن تأكدوا من Database → Replication.

---

## معيار "خلصنا"

- [ ] المكوّنات الخمسة كلها تشتغل فعلياً: Auth · Database · Storage · Realtime · Edge Functions
- [ ] RLS مفعّل ومُختبَر (مستخدم لا يرى قرارات غيره الشخصية)
- [ ] المشروع منشور على رابط يفتح من الجوال
- [ ] رابط تصويت جماعي يشتغل من جهاز ثانٍ بدون تسجيل دخول
- [ ] `README.md` يشرح خطوات التشغيل من الصفر
